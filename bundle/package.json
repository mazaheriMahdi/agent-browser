{
  "name": "agent-browser-bundle",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone Happy DOM bundle for V8 embedding",
  "scripts": {
    "build": "node build.js",
    "test": "node test/verify.js"
  },
  "dependencies": {
    "happy-dom": "^20.14.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}